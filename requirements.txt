# Add setuptools first to provide distutils
setuptools>=65.5.0

# Pin streamlit to a specific version
streamlit==1.30.0 

# Keep other dependencies
openai
anthropic

# Use pre-built wheels for problematic packages
--find-links https://download.pytorch.org/whl/torch_stable.html
--find-links https://download.lfd.uci.edu/pythonlibs/archived/cp312/
numpy==1.24.3

# Other dependencies
requests
python-dotenv
pandas
yt-dlp
beautifulsoup4
lxml
pillow

# Pin web3 and protobuf to compatible versions
web3==5.31.1
protobuf==3.19.5

# Add moviepy after numpy is installed
moviepy==1.0.3 